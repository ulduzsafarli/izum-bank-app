spring:
  liquibase:
    change-log: classpath:liquibase/changelog-master.yml
    enabled: true
    default-schema: public
  datasource:
    url: jdbc:postgresql://localhost:5432/dockerbank
    username: postgres
    password: 123456
  jpa:
    open-in-view: true
    properties:
      hibernate:
        show_sql: true
      ddl-auto: update
server:
  port: 8082
